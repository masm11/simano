GNOME
  o configure.ac の package 名は何を見ればいい?
  o icon-name は何を使えばいい?

client
  o po 対応
  o struct sockaddr_in6 を最大として扱っていた箇所、
    sockaddr_strage に変更。

server
  o automake 対応
  - そのうち FreeBSD 対応できるといいな。
  - mbox とか。
