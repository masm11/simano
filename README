Simple Mail Notification

はじめに

  これはネットワーク経由でメール通知を受け取るプログラムです。
  メールが来たら通知エリアのアイコンが変わり、バルーンが表示
  されます。

  "simano" は SImple MAil NOtification の略です。


先に注意

  メールサーバで daemon を実行するため、メールサーバにアカウントが必要
  です。

  認証はありません。daemon に接続すれば、あなたがメールを受け取ったこと
  を誰でも知ることができます。メールサーバで firewall で接続元を絞って
  やれば、「誰でも」ではなくなります。それでも嫌、という人は、このプロ
  グラムを使うことはできません。

  ~/Maildir のみ対応です。

  通知エリアにアイコンが表示され、バルーンに新着メールがある旨表示され
  るのみで、何通届いたか、誰から届いたか、などは表示されません。


動作環境

  slackware64 14.1
    xfce4 で問題なく動作。

  FreeBSD 10.0-RELEASE i386
    server は問題なく動作。
    client は未確認。

  Solaris 11.2 x86
    server は問題なく動作。
    client は GNOME 通知領域にアイコンが表示されない。

  Ubuntu GNOME 14.04.1 desktop amd64
    server は未確認。
    client はコンパイルが通らない。アイコンが表示されない。

  Debian 7.6.0 amd64
    GNOME で問題なく動作。

  Windows 8.1 Update x64 cygwin
    server は問題なく動作。
    client は通知領域にアイコンが表示されない。


コンパイル・インストール方法

  メールサーバで server ディレクトリで、
    ./configure && make
  できた simanod をどこかにコピーしてください。

  クライアントで client ディレクトリで、
    ./configure && make
  できた simano をどこかにコピーしてください。
  日本語が欲しい人は、代わりに
    ./configure --localedir=<どこか> && make
  として、po/ja.gmo を <どこか>/ja/LC_MESSAGES/simano.mo へコピーし、
  simano を好きな場所へコピーしてください。


実行方法

  まず、ポート番号を決めてください。メールサーバのファイアウォールで、
  そのポートに穴を開けてください。

  メールサーバで
    simanod -p <ポート番号>
  と実行してください。

  そしてクライアントで
    simano -s <メールサーバのホスト名> -p <ポート番号> &
  と実行してください。

  メールが来たら、通知エリアに反映され、バルーンが表示されます。server
  が linux, solaris, cygwin の場合はすぐに反映されると思いますが、その
  他の場合は1分程度の遅延が生じます。

  アイコンが気に入らなければ、-N <newmail> と -n <nomail> で変更できま
  す。


ライセンス

  COPYING を参照ください。


作者

  原野裕樹 <masm@masm11.ddo.jp>
